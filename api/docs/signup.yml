Signup.
---
tags:
  - User authentication
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This is for signingup a user
responses:
  201:
    description: user registered successfully 
    schema:
      properties:
        name:
          type: string
          default: bekeplar
          email:
            type: string
            default: bekeplar@gmail.com
            password:
              type: string
              bekk123T
    400:
      description: Registration failed!
    401:
      description: Missing Authorization Header!
security:
  - bearerAuth:
    description: Pass the jwt token e.g Bearer <token>
    type: http
    schema: bearer
    in: header
    bearerFormat: JWT                