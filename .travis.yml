language: python

python:
  - "3.6"

cache: pip
install: 
  - pip install -r requirements.txt
  - pip install coveralls

script: 
  - py.test --cov-report term-missing --cov app -v
  - coverage
  - coveralls 
  
after_success:
  - coveralls
  